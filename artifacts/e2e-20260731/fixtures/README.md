# E2E fixture

This deterministic, non-sensitive fixture is copied into each isolated
workspace.  It supports both the LaTeX and Typst report paths and makes the
agent prompts, manifests, and generated reports easy to trace.
