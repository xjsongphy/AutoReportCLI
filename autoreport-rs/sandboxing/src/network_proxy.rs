//! Managed-network implementation used by the vendored Codex sandbox.
//!
//! The implementation lives in its own crate, matching Codex's crate boundary;
//! this module preserves the sandbox backend import paths.

pub use autoreport_network_proxy::*;
