[build]
# 增量编译，复用产物以减少 target 堆积
incremental = true

# 只保留行号信息，兼顾调试与体积（0=无, 1=行号, 2=完整）
[profile.dev]
debug = 1

[profile.release]
incremental = false  # 发布构建不保留增量缓存，避免 target 长期膨胀
strip = true  # 移除符号表
lto = true    # 链接时优化，减小二进制大小
codegen-units = 1  # 更好的优化，但编译稍慢
